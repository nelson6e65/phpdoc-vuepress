## {{ constant|trim('\\') }} {{ include('badges.md.twig', { elem: constant }) }}

{{ constant.summary|replace({'#### ': '- ', '### ': '- ', '## ': '- ', '# ': '- ',})|raw }}

```php
{{ constant.name }} = {{ constant.value|raw }};
```

**Type:** **`{{ constant.types|default(['mixed'])|sort_asc|join('`** | **`')}}`**

### Description

{% if constant.description %}
{{ constant.description|replace({'#### ': '- ', '### ': '- ', '## ': '- ', '# ': '- ',})|raw }}
{% else %}
> No description.
{% endif %}

{{ include('metadata-table.md.twig', { elem: constant }) }}
